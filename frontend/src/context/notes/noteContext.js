import { createContext } from "react";
// creating notecontext using context api to create a state which can be accessed without sending props
const noteContext = createContext();

export default noteContext;